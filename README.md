# Feedme Specification Schema Tests

Tests that the schemas published in `feedme-spec` operate as expected.

To run the tests...

```
git clone https://github.com/aarong/feedme-spec-test
npm install
npm run test
```
